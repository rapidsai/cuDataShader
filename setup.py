from setuptools import setup

setup(name='cuviz',
      version='0.1',
      description='GPU accelerated visualization for Data Science',
      license='NVIDIA',
      packages=['cuviz'],
      zip_safe=False)